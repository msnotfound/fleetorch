name = "gemini"
command = "gemini"
args = ["--skip-trust", "--yolo", "-p", "{prompt}"]
prompt_arg = ""
default_budget_usd = 0
default_turns = 0
sandbox = "worktree"
streams_freely = true
notes = "Free tier eligible. 1M context. Sandboxes to cwd — pre-stage files before spawning."
